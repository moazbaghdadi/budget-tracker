{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Permissions only available on desktop builds. The updater plugin is desktop-only; mobile updates go through the platform store.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["updater:default"]
}
