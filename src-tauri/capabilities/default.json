{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability granted to the main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-app-config-read-recursive",
    "fs:allow-app-config-write-recursive",
    "fs:allow-app-config-meta-recursive",
    "dialog:default",
    "opener:default"
  ]
}
