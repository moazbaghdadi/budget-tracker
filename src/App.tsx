export default function App() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: '100vh',
        fontSize: 18,
        color: 'var(--text-muted)',
      }}
    >
      متتبع الميزانية — جاري التحضير…
    </div>
  );
}
